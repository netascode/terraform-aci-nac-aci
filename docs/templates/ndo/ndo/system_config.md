# System Config

Location in GUI:
`Infrastructure` » `System Configuration`

{{ aac_doc }}

### Examples

```yaml
ndo:
  system_config:
    banner:
      alias: Banner New
      type: informational
      message: Message New
      state: active
```

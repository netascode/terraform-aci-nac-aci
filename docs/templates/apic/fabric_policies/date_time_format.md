# Date and Time Format

Location in GUI:
`System` » `System Settings` » `Date and Time`


{{ doc_gen }}

### Examples

```yaml
apic:
  fabric_policies:
    date_time_format:
      display_format: local
      timezone: p0_UTC
      show_offset: true
```

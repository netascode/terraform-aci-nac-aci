# RBAC Node Rules

Location in GUI:
`Admin` » `AAA` » `Security` » `RBAC Rules` » `Node Rules`


{{ doc_gen }}

### Examples

```yaml
apic:
  node_policies:
    nodes:
      - id: 101
        security_domains:
          - SEC1
```

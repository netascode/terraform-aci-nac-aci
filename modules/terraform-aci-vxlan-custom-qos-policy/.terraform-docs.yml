version: '>= 0.14.0'

formatter: markdown table

content: |-
  # Terraform ACI VXLAN Custom QoS Policy

  VXLAN Custom QoS Policy

  Location in GUI:
  `Tenants` » `infra` » `Policies` » `Protocol` » `VXLAN Custom QoS Policy`

  ## Examples

  ```hcl
  {{ include "./examples/complete/main.tf" }}
  ```

  {{ .Requirements }}

  {{ .Providers }}

  {{ .Inputs }}

  {{ .Outputs }}

  {{ .Resources }}

output:
  file: README.md
  mode: replace

sort:
  enabled: false

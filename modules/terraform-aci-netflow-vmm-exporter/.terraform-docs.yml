version: '>= 0.14.0'

formatter: markdown table

content: |-
  # Terraform ACI Netflow VMM Exporter Module

  Manages ACI Netflow VMM Exporters

  Location in GUI:
  `Fabric` » `Access Policies` » `Policies` » `Interface Policies` » `NetFlow` » `Netflow Exporters for VM Networking`

  ## Examples

  ```hcl
  {{ include "./examples/complete/main.tf" }}
  ```

  {{ .Requirements }}

  {{ .Providers }}

  {{ .Inputs }}

  {{ .Outputs }}

  {{ .Resources }}

output:
  file: README.md
  mode: replace

sort:
  enabled: false

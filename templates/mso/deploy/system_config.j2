{
    "baseLockoutTime": {{ mso.system_config.lockout_time | default(defaults.mso.system_config.lockout_time) }},
    "allowedConsecutiveAttempts": {{ mso.system_config.allowed_consecutive_attempts | default(defaults.mso.system_config.allowed_consecutive_attempts) }},
    "bannerConfig": [
        {
            "alias": "{{ mso.system_config.banner.alias | default() }}",
            "banner": {
                "bannerType": "{{ mso.system_config.banner.type | default(defaults.mso.system_config.banner.type) }}",
                "message": "{{ mso.system_config.banner.message | default() }}",
                "bannerState": "{{ mso.system_config.banner.state | default(defaults.mso.system_config.banner.state) }}"
            }
        }
    ]
}